# Copie para .env e preencha. O .env nao vai para o repositorio.
# Painel do Supabase: Settings -> API

SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# service_role key — ignora RLS por completo.
# Use apenas nos scripts de carga, nunca no frontend nem na Vercel.
SUPABASE_SERVICE_KEY=
